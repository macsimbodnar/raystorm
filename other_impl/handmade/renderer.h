#pragma once
#include "common_platform.h"
#include "world.h"

void draw_world(game_offscreen_buffer_t* buffer, const world_t* world);
void draw_minimap(game_offscreen_buffer_t* buffer, const world_t* world);
